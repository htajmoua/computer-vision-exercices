# Alternative à environment.yml pour les utilisateurs pip
# Installation : python -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
#
# Pour PyTorch avec accélération GPU, suivez plutôt les instructions sur
# https://pytorch.org/get-started/locally/ avant de lancer ce requirements.

numpy>=1.26
scipy>=1.11
matplotlib>=3.8
pandas>=2.1

pillow>=10.0
scikit-image>=0.22
opencv-python>=4.8

scikit-learn>=1.4

torch>=2.2
torchvision>=0.17

jupyterlab>=4.0
ipywidgets>=8.1
tqdm>=4.66
imageio>=2.34
